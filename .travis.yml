sudo: required
dist: trusty

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

language: node_js
node_js:
  - "6"
  - "7"

cache:
  yarn: true
  directories:
  - client/node_modules
  - server/node_modules

env:
  global:
    - FACEBOOK_APP_ID=12345
    - FACEBOOK_APP_SECRET=a1b2c3d4e5f6g7

services:
  - mongodb

before_install:
  - npm install -g npm@latest local-web-server
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - cd server && yarn install
  - cd ../client && yarn install

script:
  # Server
  - cd ../server
  - npm run lint
  - npm run test

  # Client
  - cd ../client
  - npm run lint
  - npm run test
  - ng build --prod --aot
  - cd dist && ws --port 4200&
  - npm run e2e

after_script:
  - cat ./coverage/coverage.lcov | ./node_modules/coveralls/bin/coveralls.js
  - cd ../server istanbul cover ./node_modules/mocha/bin/_mocha './**/*.spec.js' --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
  - nsp check
